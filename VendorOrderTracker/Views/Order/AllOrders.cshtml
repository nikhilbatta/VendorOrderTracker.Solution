@using VendorOrderTracker.Models;

<body>
    <h1>Found your vendor and its Orders</h1>
    <h1>@Model.Name</h1>
    @foreach (Order item in @Model.listOfOrders)
    {
        <li>@item.Price</li>
    }
    <form action="/vendor/@Model.ID/neworder">
    <button type="submit"> Add An Order</button>
   </form>
<a href = "/vendors"> Go to vendors </a>
<a href = "/vendors/newvendor">Add new vendor</a>
</body>